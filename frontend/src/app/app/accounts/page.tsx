import ModuleShell from "@/components/ModuleShell";

export default function AccountsPage() {
  return (
    <ModuleShell
      title="Accounts"
      description="Manage fee structures, record payments, and unlock results when fees are marked paid."
    />
  );
}
