# DirectAdmin FTP credentials — copy to deploy/directadmin/.env.local (gitignored)
# Do not put the real password in git.
PCS_FTP_HOST=pcism.com.ng
PCS_FTP_USER=pcismcom
PCS_FTP_PASSWORD=
PCS_FTP_API_DIR=pcs-api
PCS_FTP_WEB_DIR=pcs-web
